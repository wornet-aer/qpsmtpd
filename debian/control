Source: qpsmtpd
Section: mail
Priority: optional
Maintainer: Andreas Erhard <erhard@wor.net>
Build-Depends:
 debhelper-compat (= 13),
 perl,
 po-debconf,
# used by the test suite run at build time
 libdigest-hmac-perl,
 libmailtools-perl,
 libnet-dns-perl,
 libnet-ip-perl,
 libnetaddr-ip-perl,
 libtest-output-perl,
Standards-Version: 4.7.2
Rules-Requires-Root: no
Homepage: https://github.com/smtpd/qpsmtpd
Vcs-Git: https://github.com/wornet-aer/qpsmtpd.git -b debian
Vcs-Browser: https://github.com/wornet-aer/qpsmtpd/tree/debian

Package: qpsmtpd
Architecture: all
Depends:
 adduser (>= 3.0),
 libdigest-hmac-perl,
 libio-socket-ssl-perl,
 libipc-shareable-perl,
 libmailtools-perl,
 libnet-dns-perl,
 libnet-ip-perl,
 perl (>= 5.32),
 ${misc:Depends},
 ${perl:Depends}
Recommends:
 libcdb-file-perl,
 libfile-nfslock-perl,
 libio-socket-inet6-perl,
 libmail-spf-perl (>= 2.8.0),
 libnetaddr-ip-perl,
 libsocket6-perl
Suggests:
 clamav-daemon,
 libclamav-client-perl,
 libmail-dkim-perl,
 libnet-ldap-perl,
 libredis-perl,
 libtimedate-perl,
 spamassassin (>= 3.0.0),
 tinycdb
Description: Flexible SMTP daemon for network-level spam detection
 This is a replacement SMTP daemon which installs alongside a mail delivery
 and transport system such as Exim, Postfix or Qmail, or used as an SMTP
 proxy for a remote/DMZ MTA.
 .
 The qpsmtpd damon emphasizes spam detection during the SMTP transaction,
 attempting to reach spam/nonspam decisions prior to accepting each message,
 thereby eliminating much bounce/forgery blowback.  It exploits its visbility
 into the network transaction to detect certain behaviors often exhibited by
 spam sending agents.
 .
 Qpsmtpd is written in Perl, with an extensive plugin API making it easy to
 add new features or replace functional components.

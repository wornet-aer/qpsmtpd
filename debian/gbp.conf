[DEFAULT]
# The Debian packaging lives on the 'debian' branch, which carries the
# upstream tree merged in (source format 3.0 native).  There is no separate
# orig tarball and therefore no pristine-tar branch.
debian-branch = debian
upstream-branch = master
upstream-tag = v%(version)s
pristine-tar = False
